import { type NextPage } from "next";
import PageLayout from "../components/PageLayout";

const Checkout: NextPage = () => {
  return <PageLayout pageTitle="Checkout">Checkout</PageLayout>;
};

export default Checkout;
